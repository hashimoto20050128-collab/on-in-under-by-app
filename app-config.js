/* =========================================================================
   app-config.js
   講師がルームを発行するときに必要な「利用コード」をここで設定します。
   複数登録でき、配布相手ごとに分けても構いません。
   ※これは簡易的なアクセス制限です（ソースを見れば確認できる程度の強さ）。
     本格的に制限したい場合は Firebase Authentication 等の併用を検討してください。
   ========================================================================= */
const TEACHER_CODES = ["Naoaki28"];
